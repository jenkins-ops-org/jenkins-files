pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                sh script:"bash $WORKSPACE/hello-world/scripts/helo.sh"
            }
            if( 98 < 99 ) {
           currentBuild.result = 'FALIURE'
           return
        }
    }
    }}
